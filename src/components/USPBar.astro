---
const usps = [
  {
    icon: "award",
    text: "10 jaar garantie",
    gradient: "from-blue-500 to-blue-600"
  },
  {
    icon: "leaf",
    text: "Duurzame producten",
    gradient: "from-green-500 to-green-600"
  },
  {
    icon: "star",
    text: "Luxe afwerking",
    gradient: "from-amber-500 to-amber-600"
  },
  {
    icon: "tools",
    text: "Vakkundig geplaatst",
    gradient: "from-orange-500 to-orange-600"
  }
];
---

<div class="bg-blue-600 text-white">
  <div class="container mx-auto">
    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-4 gap-4 py-2">
      {usps.map((usp, index) => (
        <div class="flex items-center justify-center sm:justify-start gap-2 p-2">
          <span class="icon-container">
            <i class={`fas fa-${usp.icon} text-xl`}></i>
          </span>
          <span>{usp.text}</span>
        </div>
      ))}
    </div>
  </div>
</div>

<style>
  .icon-container {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    transition: transform 0.3s ease;
  }

  .icon-container:hover {
    transform: scale(1.1);
  }
</style>
